# Generators allows us to generate a sequence of values over time
# e.g range(100) is a generator
# it allows us to use a special keyword called el

# A list creates a giant list of items that lives in our computer memory but range creates them one by one

# Generators allows us to generate a list without taking space in memory
