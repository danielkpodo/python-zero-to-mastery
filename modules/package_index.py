# The true power of python is because of the package index
# The python package index allows us to have access to different codes

# we are going to this with the pip install command

# the website for this package is pypi.org
# check the  standard library if packgage is there before using pypi
