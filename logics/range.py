# range func allows us to loop over an object
# example: range(step, stop, step)

for _ in range(100):  # we use underscore when we care not abt the variable for the loop
    print(_)

# to lopp backwards
for _ in range(100, 0, -10):  # start counting from 100, 90, 80, ...
    print(_)
