# Poly means many
# morphism means many forms

# Polymorphism refers to the idea in which object classes can share the same method names but those method names behave differently based on what objects call them

# Methods can be shared among classes but they differ in functionality because if the object calling them

# polymorphism is also  the ability to redefine methods for many classes
