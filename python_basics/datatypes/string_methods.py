# useful string methods
# .find(), .lower(), .upper(), .capitalize(), .join(), .split(),.strip(), lstrip(), rstrip()

# .split() => splits a string into an array, you can also split dependent on a symbol

# google for string methods

phrase = "<h1>narh</h1>"


name = ["n", "a", "r", "h"]

user_details = "".join(name)
print(user_details)
