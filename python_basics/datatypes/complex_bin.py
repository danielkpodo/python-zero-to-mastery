# complex is also another datatype
# bin is used to tell us the binary representation of a number
# e.g. bin(5) = 0b101 -> the 0b at the front tells us it a binary number

print(bin(5))

# to convert the binary back to int
print(int("0b101", 2))
